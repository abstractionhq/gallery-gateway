mutation createOtherMediaEntry($input: OtherMediaInput!) {
  createOtherMedia(input: $input) {
    id
    name
    entryCap
    entries {
      id
      student {
        username
      }
    }
  }
}
