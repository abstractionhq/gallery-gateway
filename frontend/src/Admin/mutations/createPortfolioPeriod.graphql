mutation createPortfolioPeriod($input: PortfolioPeriodInput!) {
  createPortfolioPeriod(input: $input) {
    id
    name
    description
    entryStart
    entryEnd
    judgingStart
    judgingEnd
  }
}
