query Judges {
  assigned: users(type: JUDGE) { # TODO: Change this to the correct query
    id
    username
    firstName
    lastName
  }

  unassigned: users(type: JUDGE) { # TODO: Change this to the correct query
    id
    username
    firstName
    lastName
  }
}
