query Shows {
  shows(orderBy: { sort: "entryStart", direction: DESC }) {
    id
    name
    description
    entryStart
    entryEnd
    judgingStart
    judgingEnd
    entryCap
    createdAt
    updatedAt
  }
}
